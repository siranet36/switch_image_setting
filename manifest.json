{
  "name": "A browser control images setting which changes its icon when clicked",
  "description": "Change browser control images setting when its icon is clicked",
  "permissions": [ "contentSettings" ],
  "version": "1.0",
  "background": { "scripts": ["background.js"] },
  "browser_action": {
      "name": "Click to change the icon's color"
  },
  "manifest_version": 2
}
